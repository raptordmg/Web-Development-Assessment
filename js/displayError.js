/*
    Displays a database error message
 */
function displayErrorDatabase() {
    alert("Sorry our database isn't feeling well.\nPlease try again later");
}

/*
    Displays an error message
 */
function displayError(msg) {
    alert("Error: " + msg);
}